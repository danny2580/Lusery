[build]
  command = "rm -rf .cache node_modules/.vite attached_assets dist 2>/dev/null; npm install && npm run build"
  publish = "dist/public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
